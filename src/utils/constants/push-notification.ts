export enum NOTIFICATION_CHANNEL_ID {
  MATCHES = 'so-syncd-matches',
  LIKES = 'so-syncd-likes',
  MESSAGES = 'so-syncd-messages',
}

export enum NOTIFICATION_CHANNEL_NAME {
  MATCHES = 'Matches',
  LIKES = 'Likes',
  MESSAGES = 'Messages',
}

export const channelNames = {
  'so-syncd-matches': 'Matches',
  'so-syncd-likes': 'Likes',
  'so-syncd-messages': 'Messages',
};
